[project]
name = "rag-against-the-machine"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fire>=0.7.1",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=1.19.1",
]

[[tool.mypy.overrides]]
module = "tqdm.*"
ignore_missing_imports = true
